<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rajaryan.sansadadarsh">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Intro"></activity>
        <activity android:name=".Otp" />
        <activity android:name=".Login" />
        <activity android:name=".Search" />
        <activity android:name=".Technology" />
        <activity android:name=".NodelActivity" />
        <activity android:name=".mp_details" />
        <activity android:name=".PdfView" />
        <activity android:name=".Schemes" />
        <activity android:name=".Villages" />
        <activity android:name=".Home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>